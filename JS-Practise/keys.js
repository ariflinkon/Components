const bioData = {
Name: 'Laiba Bekkol',
Character: 'Jogrute',
Pesha: 'Jogra kora',
DailyJograLimit: 10
}; 

//console.log(bioData);
const keys = Object.keys(bioData);
console.log(keys);

const x = Object.isSealed(bioData);
console.log(x);